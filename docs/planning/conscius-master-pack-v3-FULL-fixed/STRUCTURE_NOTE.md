# STRUCTURE NOTE

Any example folder structures in docs are illustrative.

Actual implementation MUST place:
`packages/runtime` and `packages/cli` at the repo root Nx workspace.

Illustrative nested `packages/` trees under `docs/planning/refactor/` must not be copied — they confuse Nx project inference.
